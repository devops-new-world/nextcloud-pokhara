name: Deploy Nextcloud

on:
  push:
    branches:
      - 'feature/nextcloud-roshan'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: dummy job
        run: |
          echo "Hello...."