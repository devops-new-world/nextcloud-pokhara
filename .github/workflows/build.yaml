name: Deploy Nextcloud

on:
  push:
    branches:
      - 'feature/nextcloud-mukta'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: dummy work
        run: |
          echo "Hello...."
      
